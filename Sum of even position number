# list of numbers
numbers = [10, 20, 30, 40, 50, 60]

sum_even_pos = 0

# loop through list
for i in range(len(numbers)):
    # check for even position (index + 1)
    if (i + 1) % 2 == 0:
        sum_even_pos += numbers[i]

print("Sum of even position elements:", sum_even_pos)
